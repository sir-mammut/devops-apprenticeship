# shellcheck disable=SC2148
# .envrc - project-specific environment loaded by direnv
# Run: `direnv allow` after adding the hook to your shell.

use asdf               # Activate asdf-managed tool versions from .tool-versions

export APP_IMAGE="devops-apprentice-app"    # Default container image name for this project
export APP_TAG="dev"                        # Default tag for the image (e.g., "dev" for local development)
export NODE_ENV="development"               # Set Node environment (could be overridden in CI to "ci" or "test")

# Example: developer override (not committed)
# export DEBUG="app:*"
