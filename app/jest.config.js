export default {
    testEnvironment: 'node',
    verbose: true,

    // We don't use Babel; disable transforms
    transform: {},

    // Optional: keep tests inside app only
    roots: ['<rootDir>'],
};
